<div class="mx-auto max-w-3xl mt-24">
  <h1 class="font-bold text-4xl mb-6"> Bitcoin data!</h1>
  <%= render "form", info: Info.new %>
  
  <%# <%== pagy_nav(@pagy) %> 
  <div id="infos" class="divide-y list-none block shadow-sm mb-4 mt-4 appearance-none bg-white border border-green-200 rounded w-full text-gray-700 mr-3 py-2 px-2 leading-tight focus:outline-none">
    <% @infos.each do |info| %>
      <%= render info %>
    <% end %>
  </div>
  <%== pagy_nav(@pagy) %>
</div>
